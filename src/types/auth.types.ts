
export type LoginFormTypes = {
    email: string;
    password: string;
};
  
export type RegisterFormTypes = {
    firstName: string;
    lastName: string;
    phone: string;
    email: string;
    role: string;
    password: string;
    language: string
}